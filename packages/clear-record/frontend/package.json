{
  "name": "clear-record-frontend",
  "private": true,
  "type": "module",
  "description": "Build pipeline for the clear-record web console assets (htmx + Alpine + Tailwind v4, bundled by Vite). Compiled output is committed under src/clear_record/web/static/; end users never need Node.",
  "scripts": {
    "build": "vite build",
    "e2e": "playwright test",
    "e2e:install": "playwright install chromium"
  },
  "dependencies": {
    "alpinejs": "3.17.3",
    "htmx.org": "4.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@tailwindcss/vite": "^4",
    "tailwindcss": "^4",
    "vite": "^7"
  }
}
